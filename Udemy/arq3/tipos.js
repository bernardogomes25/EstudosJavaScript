//String, number, undefined, null, boolean, symbol

const nome = 'Gomes'; //String
const num = 25; //Number
let indefinido; //Undefined -> Não aponta para lugar nenhum na memória
let nulo = null; //Nulo -> Não aponta para lugar nenhum na memória
const aprovado = true; //Boolean
const caixa = [1,2,3]; //Array

console.log(typeof(nome));
console.log(typeof(num));
console.log(typeof(indefinido));
console.log(typeof(nulo));
console.log(typeof(aprovado));
console.log(typeof(caixa));